<!DOCTYPE html>
<html>
<head>
    <title>Admin Dashboard</title>
    <link rel="icon" type="image/x-icon" href="/favicon.ico">
</head>
<body>

<h1>Admin Dashboard</h1>

<p>Total Users: <strong><%= totalUsers %></strong></p>

<table border="1" cellpadding="8">
    <tr>
        <th>Username</th>
        <th>Email</th>
        <th>Country</th>
        <th>Referrer</th>
        <th>Admin</th>
    </tr>

    <% users.forEach(user => { %>
        <tr>
            <td><%= user.username %></td>
            <td><%= user.email %></td>
            <td><%= user.country %></td>
            <td><%= user.referrer ? user.referrer.username : "None" %></td>
            <td><%= user.isAdmin ? "YES" : "NO" %></td>
        </tr>
    <% }) %>
</table>

<a href="/home">Back to Home</a>

</body>
</html>
